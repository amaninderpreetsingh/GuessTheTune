{
  "name": "guessthetune-server",
  "version": "1.0.0",
  "description": "Backend server for GuessTheTune music trivia game",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy": "gcloud run deploy guessthetune-backend --source . --platform managed --region us-central1 --allow-unauthenticated --timeout 3600 --max-instances 10 --memory 512Mi"
  },
  "keywords": ["spotify", "music", "trivia", "game", "websocket"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.0",
    "cors": "^2.8.5",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "cookie-parser": "^1.4.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
